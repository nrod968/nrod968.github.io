{
  "homepage": "https://nrod968.github.io/",
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "install": "^0.13.0",
    "npm": "^11.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.1",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "set NODE_OPTIONS=--openssl-legacy-provider && npm run build",
    "deploy": "set NODE_OPTIONS=--openssl-legacy-provider && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
